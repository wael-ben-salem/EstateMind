# Copy to `.env` and fill in real values, then `docker compose up -d lawagent`.
# Compose auto-loads `.env` from this folder; values are injected into containers.

# --- Member 2's law agent (mohamedabdennadher/realestate-law-agent) ---
# Get from member 2, OR create your own free-tier accounts:
#   Qdrant Cloud:  https://cloud.qdrant.io
#   Cohere:        https://dashboard.cohere.com/api-keys
QDRANT_URL=https://your-cluster-id.eu-west-1-0.aws.cloud.qdrant.io
QDRANT_API_KEY=
COHERE_API_KEY=
