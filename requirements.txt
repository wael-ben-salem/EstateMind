# ── Streamlit app ──────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── PyTorch (installed separately by launch scripts — see below) ───────────────
# The launch scripts detect your GPU and install the right wheel automatically.
# If you install manually:
#   NVIDIA GPU (CUDA 12.x):  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   CPU only:                pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.7.0
torchvision>=0.22.0

# ── Image ──────────────────────────────────────────────────────────────────────
Pillow>=10.0.0

# ── BLIP-2 (downloads ~15 GB on first run, cached afterwards) ─────────────────
transformers>=4.36.0
accelerate>=0.26.0

# ── CLIP encoder support (only needed if SAT bundle uses CLIP backbone) ────────
openai-clip>=1.0.1
