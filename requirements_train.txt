# Extra dependencies needed only for re-training (not for running the app).
# Install on top of requirements.txt:
#   pip install -r requirements.txt
#   pip install -r requirements_train.txt

kagglehub>=0.3.0
nltk>=3.8.1
tqdm>=4.66.0
pandas>=2.0.0
matplotlib>=3.7.0
ipywidgets>=8.0.0
jupyter>=1.0.0
notebook>=7.0.0
