version: "3.9"

services:
  recommender:
    build: .
    container_name: estatemind-recommender
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
